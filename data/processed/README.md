# data/processed/

Intermediate and analytic datasets written by the `R/` scripts (`.rds` files).
Git-ignored because these are fully reproducible from `data/raw/` — regenerate
them by running the pipeline (`R/99_run_all.R` or the numbered scripts in order).
